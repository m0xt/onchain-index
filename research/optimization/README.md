# Phase D optimization research

Canonical runnable modules live under `src/onchain_index/research/optimization/` so they can import the package cleanly via `uv run python -m ...`.

This top-level directory is intentionally non-production documentation space only. Optimization outputs are written to `.cache/optim/*.json` and summarized in `reports/phase-d-optimization-2026-05-21.md`.
